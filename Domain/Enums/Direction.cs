namespace p07_vimkeys_game.Domain.ValueObjects;

/// <summary>
/// Enum representing the four possible movement directions
/// Corresponds to VIM keys: k=Up, j=Down, h=Left, l=Right
/// </summary>
public enum Direction
{
    None,
    Up,
    Down,
    Left,
    Right
}

internal static class EnumHelper
{
  /*
   * TODO: function 1
   * pass in generic enum type
   * return an array of enum type values
   * option to include None (zero value enum), defaults exclude
   * */

  /*
   * TODO: function 2
   * pass in generic enum type
   * return an array of enum string literal
   * option to include None (zero value enum), defaults exclude
   * */
}
